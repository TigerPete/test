apiVersion: v1
kind: Service
metadata:
  name: rag-query
  namespace: rag
  labels:
    app.kubernetes.io/name: rag-query
spec:
  selector:
    app.kubernetes.io/name: rag-query
  ports:
    - name: http
      port: 8000
      targetPort: 8000
