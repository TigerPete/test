apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rag
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - ollama-service.yaml
  - ollama-deployment.yaml
  - query-service.yaml
  - query-deployment.yaml
  # ingest-job.yaml — apply manually after PDFs are on the PVC (see README)
